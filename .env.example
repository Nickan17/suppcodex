# ------------------------------------------------------------------------------
# --                       Supabase Environment Variables                       --
# ------------------------------------------------------------------------------
#
# This file serves as a template for the required environment variables.
#
# 1. COPY THIS FILE
#    cp .env.example .env.local
#
# 2. FILL IN THE VALUES
#    Get these from your Supabase project's dashboard under:
#    Project Settings > API
#
# 3. KEEP IT SECRET
#    The `.env.local` file is included in `.gitignore` and should never be
#    committed to version control.
#
# ------------------------------------------------------------------------------

# The unique URL for your Supabase project.
SUPABASE_URL=https://<your-project-ref>.supabase.co

# The anonymous key for your project. This is safe to expose in a browser.
SUPABASE_ANON_KEY=<your-anon-key>

# The service role key for your project. This must be kept secret and is for
# server-to-server communication only.
SUPABASE_SERVICE_ROLE_KEY=<your-service-role-key>

# The JWT secret for your project. Used to sign and verify JWTs.
# This must be kept secret.
SUPABASE_JWT_SECRET=<your-jwt-secret>